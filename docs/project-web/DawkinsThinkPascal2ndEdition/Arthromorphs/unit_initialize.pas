unit unit_initialize;interface	uses		unit_my_globals, unit_ted, unit_richard, unit_breeding_window;	procedure MyInit;implementation        uses TextEdit, Quickdraw, SegLoad;        var		DocumentMessage, DocumentCount: integer;	procedure MyInit;		var			theInput: TEHandle;	begin		thickscale := 1;		wantColor := false;		sideways := false;		resizing := false;		centring := false;		verticalOffset := 0;		HorizontalOffset := 0;		overlap := 1.0; {in case animal has no value}		NRows := 3;		NCols := 5; {Defaults}		NBoxes := NRows * NCols;		MidBox := 1 + (NRows * NCols) div 2;		upregion := NewRgn;		InitBoneyard;		CountAppFiles(DocumentMessage, DocumentCount);		if DocumentCount > 0 then {at least one biomorph double-clicked}			begin				StartDocument;			end;		startingUp := true;		MakeAllBodyMutations(true);		MakeAllAtomMutations(true);		mutationPressure := zero;		FocusOfAttention := AnySegment;		Open_Breeding_Window(theInput);		Breed;	end;end.